<Window x:Class="ClientGUI.JoinRoomWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ClientGUI"
        mc:Ignorable="d"
        Title="JoinRoom" Height="450" Width="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="5*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Bold>List Of Rooms:</Bold>
        </TextBlock>

        <ListBox Grid.Row="1" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="20" Height="250" Width="400"/>
        
        <Button x:Name="Join" Click="Join_Click" Grid.Row="2" HorizontalAlignment="Center" FontSize="20" Height="35" Width="125" Margin="10">
            <Bold>Join</Bold>
        </Button>

        <Button Name="Return" Click="Return_Click" HorizontalAlignment="Right" VerticalAlignment="Center" FontSize="20" Margin="10">
            <Bold>Return</Bold>
        </Button>
    </Grid>
</Window>
